{
  "name": "mugshots-client",
  "version": "0.0.1",
  "main": "src/index.ts",
  "description": "Unofficial mugshots.com node client.",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "lint": "tslint -c tslint.json -p tsconfig.json"
  },
  "devDependencies": {
    "jest": "^24.1.0",
    "ts-jest": "^23.10.4",
    "tslint": "^5.13.1"
  },
  "dependencies": {
    "@types/node": "^10.12.0",
    "@types/jest": "^23.3.9",
    "@types/puppeteer": "^1.9.0",
    "typescript": "^3.3.3333",
    "puppeteer": "^1.9.0"
  }
}
